# Segment 020 `ahya-` title test

Status: test only. Publication forbidden.

Candidate:

`I Cantië yassë Prime Intellect ahya`

Literal back-translation: **The Pattern in which Prime Intellect changes.**

Parse:

- `i Cantië`: the pattern; `cantië` at NM/287–288.
- `ya-ssë`: relative “which” plus singular locative, “in which”.
- `Prime Intellect`: protected subject name.
- `ahya`: aorist test form of `ahya-`, “changes”; PM/395.

Construction control:

- declined relative `ya` is used when the relative carries a noun case;
- `yassen i eleni tintilar`, “wherein the stars tremble”, is attested at LotR/377;
- singular `yassë` is a documented test formation, not claimed as directly attested here;
- `ahya-` is used intransitively, so the candidate does not assume a direct object.

Internal result:

- short heading: conditional pass;
- philosophical context: conditional pass;
- literary guide/path context: conditional pass;
- complete title: not passed;
- external morphology review: pending;
- production source changed: no.

`Ahyaliëo` remains rejected. Next: verify the verb class and aorist of `ahya-`, then test the grace/release subtitle separately.

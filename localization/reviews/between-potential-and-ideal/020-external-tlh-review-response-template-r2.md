# Segment 020 external Klingon review response — round 2

Status: blank response template. This file is not a review and grants no approval.

Reviewer:

Review date:

Declared expertise and canonical source access:

Reviewed packet commit:

- expected packet: `020-external-specialist-review-packet-r2.md`
- expected packet commit: `fc1867f8e94bf588d8e3f2d9954207444a5083f8`

Language profile reviewed: **Okrand-canonical-only**.

## Decision vocabulary

Use exactly one for each item:

- `PASS`
- `PASS WITH CORRECTION`
- `REJECT`
- `INSUFFICIENT EVIDENCE`

Do not delete unanswered items. Use `INSUFFICIENT EVIDENCE` when the exact Okrand source cannot be checked.

## TLH-01 — main title

Candidate: `Prime Intellect choHbogh mIw`

Decision:

Corrected form, if any:

Word-by-word and suffix parse:

Exact canonical lexical sources:

Exact grammar sources:

Literal back-translation:

Idiomatic back-translation:

Alternative or dangerous readings:

Specific answers:

1. `mIw` is unambiguously head and subject:
2. protected name is unambiguously object:
3. zero prefix valid:
4. `mIw` carries philosophical process rather than only technical procedure:
5. title styling acceptable:

## TLH-02 — mercy and participant action

Candidate: `pung: vang yInbogh ghot 'e' chaw' Prime Intellect`

Decision:

Corrected form, if any:

Word-by-word and suffix parse:

Exact canonical lexical sources:

Exact grammar sources:

Literal back-translation:

Idiomatic back-translation:

Alternative or dangerous readings:

Specific answers:

1. `vang yInbogh ghot` natural and unambiguous:
2. `'e'` scope makes the event the object:
3. living person is not object of processing/control:
4. `pung` plus clause means restraint rather than pity:
5. subtitle register acceptable:

## TLH-03 — output versus agency

Candidates:

- `jatlh Prime Intellect.`
- `vang yInbogh ghot.`

Decision:

Corrected forms, if any:

Clause-by-clause parse:

Exact canonical lexical sources:

Exact grammar sources:

Literal back-translations:

Idiomatic back-translations:

Alternative or dangerous readings:

Specific answers:

1. verb-subject order natural:
2. `jatlh` states speech without overclaiming answer/knowledge:
3. `vang` preserves the living participant as actor:
4. repeating the protected name is preferable to classification by pronoun/noun:

## TLH-04 — capability without worth

Candidate: `jatlhlaH Prime Intellect.`

Decision:

Corrected form, if any:

Word-by-word and suffix parse:

Exact canonical lexical sources:

Exact grammar sources:

Literal back-translation:

Idiomatic back-translation:

Alternative or dangerous readings:

Specific answers:

1. `-laH` attachment and scope correct:
2. only capability is asserted:
3. no implication of usefulness, correctness, success, life, or worth:

## TLH-05 — stop before control

Candidate: `SeHpa' Prime Intellect, mev Prime Intellect.`

Decision:

Corrected form, if any:

Clause-by-clause parse:

Exact canonical lexical sources:

Exact grammar sources:

Literal back-translation:

Idiomatic back-translation:

Alternative or dangerous readings:

Specific answers:

1. `-pa'` scope correct:
2. both subjects unambiguous:
3. contextual “help becomes control” reading defensible:
4. explicit help clause required:
5. voluntary-restraint construction recommendation:

## TLH-06 — nonreplacement

Current role sequence:

- `jatlh Prime Intellect.`
- `vang yInbogh ghot.`
- `vang yInbogh ghot 'e' chaw' Prime Intellect.`

Decision:

Direct “replace / take another's place / act instead of” construction, if any:

Exact canonical lexical sources:

Exact grammar sources:

Full parse:

Literal back-translation:

Idiomatic back-translation:

Alternative or dangerous readings:

Specific answers:

1. role separation sufficient in context:
2. Okrand-canonical direct replacement construction available:
3. explicit negative possible without claiming total inactivity:
4. direct form improves or harms semantic accuracy:

## TLH-07 — source-locator ledger

For every form retained, provide the most precise canonical locator available.

| Form | Meaning used | Source work | Page/section/event | Canon status | Notes |
|---|---|---|---|---|---|
| `mIw` | process/procedure | | | | |
| `choH` | change/alter | | | | |
| `-bogh` | relative clause | | | | |
| `pung` | mercy | | | | |
| `vang` | act | | | | |
| `yIn` | live | | | | |
| `ghot` | person | | | | |
| `'e'` | sentence as object | | | | |
| `chaw'` | allow/permit | | | | |
| `jatlh` | say/speak | | | | |
| `-laH` | can/be able | | | | |
| `SeH` | control | | | | |
| `-pa'` | before | | | | |
| `mev` | stop/cease | | | | |

Do not cite only community lists where the original Okrand source is available.

## Overall Klingon decision

Complete title candidate:

- `PASS`
- `PASS WITH CORRECTION`
- `REJECT`
- `INSUFFICIENT EVIDENCE`

Opening proposition architecture:

- `PASS`
- `PASS WITH CORRECTION`
- `REJECT`
- `INSUFFICIENT EVIDENCE`

May production source be changed now: **NO** unless the repository's body, parity, and independent-review gates also pass.

Blocking findings:

Required corrections:

Forms explicitly prohibited from production:

Reviewer attestation:

> I checked the cited Okrand sources or explicitly marked every unavailable source as insufficient evidence. I did not approve forms solely because they were understandable, common in community usage, or aesthetically plausible.

Reviewer signature/name:

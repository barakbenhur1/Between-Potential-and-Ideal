BPI V41 — Symmetric tab/media image padding patch

This ZIP contains changed/new files only.

Apply:
1. Extract this ZIP over the existing V40 product folder.
2. Approve overwrite/replace for matching files.
3. No images, PDF, DOCX, or MD content documents are changed.

Fix:
- Makes white padding around tab/page media images symmetric on all sides.
- Adds late CSS override in relevant HTML pages because many pages contain inline style blocks after styles.css.
- Updates styles.css and styles-home-original.css.

